{
  "name": "@breason/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc -p tsconfig.json --noEmit",
    "build": "echo 'types package: no build step'",
    "lint": "echo 'types package: no lint step'"
  }
}
