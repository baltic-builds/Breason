{
  "name": "@breason/prompts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "echo 'prompts package: no build step'",
    "lint": "echo 'prompts package: no lint step'"
  },
  "dependencies": {
    "@breason/types": "*"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "typescript": "^5.8.3"
  }
}
